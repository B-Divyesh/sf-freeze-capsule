[
  {
    "id": "sample-report",
    "claim": "The bundled sample renders journal, graphics, process, and display-session evidence.",
    "where": "landing preview, demo, README",
    "test": "npm run test:site -- --grep @claim:sample-report",
    "sandbox": "fresh browser context at /demo; run and reset the sample"
  },
  {
    "id": "demo-private",
    "claim": "The browser demo saves only demo state and makes no third-party request.",
    "where": "demo banner, privacy page",
    "test": "npm run test:site -- --grep @claim:demo-private",
    "sandbox": "fresh browser context; record every request during the demo flow"
  },
  {
    "id": "encrypted-redacted",
    "claim": "The CLI demo writes an encrypted capsule and a redacted report with journal, graphics, process, and display-session evidence.",
    "where": "landing facts, workflow, README",
    "test": "npm run test:site -- --grep @claim:encrypted-redacted",
    "sandbox": "bundled sample through freeze-capsule demo in a new temporary directory"
  },
  {
    "id": "demo-capture-render",
    "claim": "The CLI demo executes the encrypted capture and render path in a temporary directory.",
    "where": "landing sample terminal",
    "test": "npm run test:site -- --grep @claim:demo-capture-render",
    "sandbox": "bundled sample through freeze-capsule --json demo; assert the temporary capsule and report exist"
  },
  {
    "id": "redaction-coverage",
    "claim": "Rendering replaces home paths, email addresses, IP addresses, and common secret values.",
    "where": "landing workflow, README, privacy page",
    "test": "npm run test:site -- --grep @claim:redaction-coverage",
    "sandbox": "known private-value fixture through the report redactor"
  },
  {
    "id": "bounded-retention",
    "claim": "Freeze Capsule keeps no more than eight retained capsules.",
    "where": "landing facts, README",
    "test": "npm run test:site -- --grep @claim:bounded-retention",
    "sandbox": "temporary capsule directory seeded with twelve encrypted sample capsules"
  },
  {
    "id": "watchdog-gap",
    "claim": "A scheduling gap promotes the last completed rolling snapshot.",
    "where": "landing workflow, README",
    "test": "npm run test:site -- --grep @claim:watchdog-gap",
    "sandbox": "new temporary directory; suspend the real watcher process beyond its configured timeout"
  },
  {
    "id": "rolling-snapshot",
    "claim": "The user service records a bounded ten-minute window every 30 seconds.",
    "where": "landing workflow",
    "test": "npm run test:site -- --grep @claim:rolling-snapshot",
    "sandbox": "compiled service-unit and snapshot configuration"
  },
  {
    "id": "cli-local-only",
    "claim": "The CLI has no telemetry or network client; its demo never writes the normal capsule directory.",
    "where": "landing facts, privacy page, README",
    "test": "npm run test:site -- --grep @claim:cli-local-only",
    "sandbox": "CLI demo with network connect() blocked and an isolated normal-capsule directory"
  },
  {
    "id": "free-license",
    "claim": "Freeze Capsule is free and open source under the MIT License.",
    "where": "landing facts, README, terms",
    "test": "npm run test:site -- --grep @claim:free-license",
    "sandbox": "clean repository; inspect the shipped license"
  }
]
