{
  "name": "freeze-capsule-site",
  "private": true,
  "version": "0.1.1",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "generate:demo": "node tools/generate-demo-fixture.mjs",
    "build:site": "npm run generate:demo && tsc --noEmit && vite build",
    "build": "npm run build:site",
    "preview": "vite preview --host 127.0.0.1",
    "test:rust": "cargo test && sh tests/watchdog.sh",
    "test:site": "npm run build:site && playwright test",
    "test": "npm run test:rust && npm run test:site"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.13.0",
    "@playwright/test": "1.58.2",
    "@types/node": "24.3.0",
    "typescript": "5.9.3",
    "vite": "7.3.6",
    "yaml": "2.9.0"
  }
}
